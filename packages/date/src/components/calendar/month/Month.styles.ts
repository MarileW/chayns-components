import { motion } from 'framer-motion';
import styled from 'styled-components';

export const StyledMotionMonth = styled(motion.div)`
    height: 100%;
`;

export const StyledMonthName = styled.div`
    display: flex;
    align-items: center;
`;
